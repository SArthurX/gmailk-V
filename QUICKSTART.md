# 快速使用指南 - 人臉資料庫系統

## 🚀 快速開始

### 1. 編譯程式
```bash
./build.sh
```

### 2. 執行程式
```bash
./build/main models/scrfd_det_face_432_768_INT8_cv181x.cvimodel \
             models/mobilefacenet.param \
             models/mobilefacenet.bin
```

## 📝 操作說明

### 自動識別流程

**無需按鈕操作！** 系統會自動識別：

1. **直視鎖定**：將人臉對準畫面中心（黃框）
2. **自動鎖定**：保持在中心 **3 秒** 後自動鎖定（變紅框）
3. **自動提取**：立即開始提取特徵向量
4. **自動比對**：與資料庫比對並顯示結果

### 按鈕功能

| 操作 | 功能 | 說明 |
|------|------|------|
| **短按** | 重新識別 | 清除當前結果，重新提取特徵並比對 |
| **長按** (>2秒) | 註冊人臉 | 將當前鎖定的人臉註冊到資料庫 |

### 框線顏色

| 顏色 | 狀態 |
|------|------|
| 🔴 紅色 | 已鎖定（識別中或已識別） |
| 🟡 黃色 | 在中心準星（等待自動鎖定） |
| 🟢 綠色 | 穩定追蹤 |
| 🔵 藍色 | 一般追蹤 |

### 顯示資訊

- **已識別**：`姓名 (0.85)` - 綠色文字
- **未識別**：`Unknown` - 黃色文字

## 📖 使用流程

### 註冊新人臉

1. **對準中心**：將人臉移到畫面中心（黃框）
2. **保持穩定**：直視 3 秒，等待自動鎖定
3. **自動識別**：系統自動提取特徵並比對
4. **顯示結果**：看到 "Unknown"（因為尚未註冊）
5. **長按註冊**：長按按鈕 >2 秒
6. **完成**：系統自動分配姓名並儲存

### 識別已註冊人臉

1. **對準中心**：將已註冊人臉移到中心
2. **自動鎖定**：保持 3 秒等待自動鎖定
3. **自動顯示**：系統自動顯示姓名和相似度

### 重新識別

如果識別結果不正確：
1. **短按按鈕**：清除當前結果
2. **自動重試**：系統立即重新提取並比對

## 🔧 進階設定

### 修改自動鎖定時間
編輯 `src/tdl_handler.cpp` 中自動鎖定邏輯：
```cpp
// 如果在中心超過3秒且未被鎖定，自動鎖定
if (duration >= 3 && !is_already_locked) {  // 改為其他秒數
```

### 修改相似度閾值
編輯 `src/main.cpp`：
```cpp
FaceDatabase_Init(&stFaceDatabase, db_path, 0.4f);  // 改為 0.3 ~ 0.6
```

### 自訂測試姓名
編輯 `src/tdl_handler.cpp`：
```cpp
const char* test_names[] = {"Alice", "Bob", "Charlie", ...};
```

### 資料庫位置
預設：`data/face_database.json`
- 如果目錄不存在，系統會自動創建

## ❓ 常見問題

**Q: 為什麼識別不到？**  
A: 調低閾值（如 0.3），確保光線充足，人臉清晰

**Q: 如何刪除已註冊人臉？**  
A: 直接編輯 `data/face_database.json` 刪除對應條目，或刪除整個檔案重新開始

**Q: 運行中修改資料庫會讀取到嗎？**  
A: **不會**。程式只在啟動時讀取一次資料庫。修改後需要重新啟動程式才能讀取最新內容。儲存（註冊）時會立即寫入檔案。

**Q: 可以註冊多少人？**  
A: 理論無限制，但比對速度會隨人數增加而變慢

**Q: 姓名可以改嗎？**  
A: 目前使用預設姓名，後續會支援自訂輸入

**Q: 自動鎖定太慢/太快？**  
A: 可以修改程式碼中的 `duration >= 3` 秒數

## 📊 系統架構

```
┌─────────────┐
│   Camera    │
└──────┬──────┘
       │
┌──────▼──────────┐
│  Face Detection │
│   (SCRFD Face)  │
└──────┬──────────┘
       │
┌──────▼──────────┐      ┌─────────────┐
│  Auto-Lock      │      │   Button    │
│  (3s at center) │◄────►│   Handler   │
└──────┬──────────┘      │  (Re-ID/Reg)│
       │                 └─────────────┘
┌──────▼──────────┐
│ Feature Extract │
│ (MobileFaceNet) │
└──────┬──────────┘
       │
┌──────▼──────────┐      ┌─────────────┐
│  Face Database  │◄────►│   Display   │
│    (JSON)       │      │ (OLED/Video)│
└─────────────────┘      └─────────────┘
```

---

需要幫助？查看完整文檔：`data/FACE_DATABASE.md`
