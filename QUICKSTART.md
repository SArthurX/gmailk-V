# 快速使用指南 - 人臉資料庫系統

## 🚀 快速開始

### 1. 編譯程式
```bash
./build.sh
```

### 2. 執行程式
```bash
./build/main models/scrfd_det_face_432_768_INT8_cv181x.cvimodel \
             models/mobilefacenet.param \
             models/mobilefacenet.bin
```

## 📝 操作說明

### 按鈕功能

| 操作 | 功能 | 說明 |
|------|------|------|
| **短按** (<3秒) | 鎖定人臉 | 鎖定中心的人臉，等待3秒後提取特徵並比對 |
| **長按** (>3秒) | 註冊人臉 | 將已鎖定的人臉註冊到資料庫 |

### 框線顏色

| 顏色 | 狀態 |
|------|------|
| 🔴 紅色 | 已鎖定（識別中） |
| 🟡 黃色 | 在中心準星（可鎖定） |
| 🟢 綠色 | 穩定追蹤 |
| 🔵 藍色 | 一般追蹤 |

### 顯示資訊

- **已識別**：`姓名 (0.85)` - 綠色文字
- **未識別**：`Unknown` - 黃色文字
- **等待中**：`Wait 2s` - 黃色文字

## 📖 使用流程

### 註冊新人臉

1. 將人臉移到畫面中心（黃框）
2. 短按按鈕鎖定（變紅框）
3. 保持穩定 3 秒
4. 看到 "Unknown" 後
5. 長按按鈕註冊
6. 完成！系統自動分配姓名

### 識別已註冊人臉

1. 已註冊人臉出現在畫面
2. 短按按鈕鎖定
3. 等待 3 秒
4. 自動顯示姓名和相似度

## 🔧 進階設定

### 修改相似度閾值
編輯 `src/main.cpp`：
```cpp
FaceDatabase_Init(&stFaceDatabase, db_path, 0.4f);  // 改為 0.3 ~ 0.6
```

### 自訂測試姓名
編輯 `src/tdl_handler.cpp`：
```cpp
const char* test_names[] = {"Alice", "Bob", "Charlie", ...};
```

### 資料庫位置
預設：`data/face_database.json`

## ❓ 常見問題

**Q: 為什麼識別不到？**  
A: 調低閾值（如 0.3），確保光線充足，人臉清晰

**Q: 如何刪除已註冊人臉？**  
A: 直接編輯 `data/face_database.json` 刪除對應條目

**Q: 可以註冊多少人？**  
A: 理論無限制，但比對速度會隨人數增加而變慢

**Q: 姓名可以改嗎？**  
A: 目前使用預設姓名，後續會支援自訂輸入

## 📊 系統架構

```
┌─────────────┐
│   Camera    │
└──────┬──────┘
       │
┌──────▼──────────┐
│  Face Detection │
│   (SCRFD Face)  │
└──────┬──────────┘
       │
┌──────▼──────────┐      ┌─────────────┐
│ Feature Extract │◄────►│   Button    │
│ (MobileFaceNet) │      │   Handler   │
└──────┬──────────┘      └─────────────┘
       │
┌──────▼──────────┐      ┌─────────────┐
│  Face Database  │◄────►│   Display   │
│    (JSON)       │      │ (OLED/Video)│
└─────────────────┘      └─────────────┘
```

---

需要幫助？查看完整文檔：`data/FACE_DATABASE.md`
